# EsyLuban 覆盖基线说明

该目录用于保存 `Projects/EsyLuban_Example/TestOutputs/json_nol10n` 的基线快照。

## 刷新方式

运行：`scripts/refresh_coverage_baseline.bat`

刷新后会在 `baseline_log.md` 追加一条记录（时间 + 来源）。

## 回滚方式

1. 直接替换该目录为历史备份。
2. 或用版本控制回退该目录的变更。
